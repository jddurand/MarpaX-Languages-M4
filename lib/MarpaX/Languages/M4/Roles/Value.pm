use Moops;

# PODCLASSNAME

role MarpaX::Languages::M4::Roles::Value {

    # VERSION

    requires 'concat';
    requires 'push';
    requires 'unshift';
    requires 'elements';
    requires 'firstElement';
}
